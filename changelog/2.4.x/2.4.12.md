---
summary: "mcp-ts-core ^0.9.1 → ^0.9.6, zod added as direct dep, manifest.json + .mcpbignore scaffolded for MCPB bundle support, install badges added to README."
breaking: false
security: false
---

# 2.4.12 — 2026-05-23

## Added

- **`manifest.json`** — MCPB bundle manifest for one-click Claude Desktop install; scaffolds `user_config` entries for all CT_* and MCP_* env vars.
- **`.mcpbignore`** — excludes non-bundle assets from `.mcpb` pack.
- **Install badges in README** — Claude Desktop and VS Code one-click install buttons.

## Changed

- **`@cyanheads/mcp-ts-core` ^0.9.1 → ^0.9.6** — picks up context elicitation, typed error contracts, `ctx.fail()`, `lint:packaging` rule, and MCPB bundle tooling from intermediate releases.
- **`zod` ^4.4.3** — added as direct dependency (was previously a transitive dep via mcp-ts-core; now required directly for schema definitions).
