---
summary: "clinicaltrials_get_field_definitions path_not_found recovery hint now names the current mode-based call shape instead of the removed no-args overview; docs/design.md synced to mode/query/path/limit/includeIndexedOnly"
breaking: false
security: false
---

# 2.8.2 — 2026-07-09

## Fixed

- **`clinicaltrials_get_field_definitions` `path_not_found` recovery hint** — now names the current `mode`-based call shape (`mode="search"` with `query="phase"` or `"enrollment"`, or `mode="overview"` for the top-level sections) instead of the pre-#49 "omit both arguments" no-args overview, which fails input validation because `mode` is required. `docs/design.md` synced to the current `mode`/`query`/`path`/`limit`/`includeIndexedOnly` parameter set (tool description, parameter table, behavior section, example workflows, implementation checklist). ([#87](https://github.com/cyanheads/clinicaltrialsgov-mcp-server/issues/87))
